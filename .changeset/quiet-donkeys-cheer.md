---
"biome-one-liner-plugin": patch
---

Declare `@biomejs/biome` as a peer dependency (`>=2.5.3`) so consumers get a machine-enforced Biome version requirement, and fix two stale doc references: the README link to the test runner (now `scripts/run-tests.mjs`) and an `oneLiner.grit` comment pointing at a script that no longer exists.
